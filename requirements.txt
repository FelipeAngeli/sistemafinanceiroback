# Core
python-dotenv>=1.0.0

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0  # Servidor WSGI/ASGI para produção

# Validation & Settings
pydantic>=2.5.0
pydantic-settings>=2.0.0  # Para Settings com env vars
email-validator>=2.0.0    # Para EmailStr do Pydantic

# Database
sqlalchemy>=2.0.0
greenlet>=3.0.0    # Necessário para SQLAlchemy async
aiosqlite>=0.20.0  # SQLite async (desenvolvimento)
asyncpg>=0.29.0    # PostgreSQL async (produção - necessário para Render PostgreSQL)

# AWS Lambda (descomentar para deploy)
# mangum>=0.17.0   # Adaptador ASGI para Lambda
# boto3>=1.33.0    # SDK AWS

# Authentication & Security
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4  # Password hashing
bcrypt>=4.0.1  # Bcrypt algorithm

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0  # Cliente HTTP async para testes de API

# Dev tools
black>=23.11.0
ruff>=0.1.6
mypy>=1.7.0
